---
# defaults file for clip.hpc.doca
doca_version: '3.0.0' # 2.9 is LTS, last to support ConnectX-4, 3 years for bug fixes and CVE updates
doca_profile: doca-ofed
doca_repo_url: "https://linux.mellanox.com/public/repo/doca/{{ doca_version }}/rhel{{ ansible_distribution_version }}/{{ ansible_architecture }}/"
doca_use_upstream_repo: true
