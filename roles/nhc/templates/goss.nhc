#!/bin/sh

DEBUG=""


function check_goss() {

  if [ -n "$DEBUG" ]; then echo "running goss tests" ; fi
  /usr/local/bin/goss-linux-amd64 --gossfile /usr/local/bin/goss_compute.yml  --vars /usr/local/bin/goss_vars.yml validate --format silent
  GOSS_RESULT=$?

  return $?
}
