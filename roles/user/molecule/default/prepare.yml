---
- name: Prepare
  hosts: all
  gather_facts: false
  tasks:
    - name: Install sshd
      ansible.builtin.yum:
        name: openssh-server
        state: present

    - name: Enable sshd
      ansible.builtin.service:
        name: sshd
        state: started
